# ==============================================================
# NutMeals — API Gateway
# ==============================================================
# The gateway is a pure HTTP reverse proxy with JWT auth and
# Redis rate limiting. It has no database, no task queue, and
# never parses user input — keep dependencies minimal.
# ==============================================================

# -- Web framework ---------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6        # [standard] includes uvloop + httptools

# -- ASGI / HTTP -----------------------------------------------
httpx[http2]==0.27.2             # [http2] installs the h2 library
h2==4.1.0                        # explicit pin — required for HTTP/2 support

# -- Auth ------------------------------------------------------
python-jose[cryptography]==3.3.0 # JWT decode/verify

# -- Settings --------------------------------------------------
pydantic==2.7.4
pydantic-settings==2.3.4         # reads .env via pydantic — replaces python-dotenv

# -- Redis -----------------------------------------------------
redis==5.0.7                     # includes redis.asyncio

# -- Logging ---------------------------------------------------
python-json-logger==2.0.7        # structured JSON logs for production
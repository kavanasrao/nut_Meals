name: User Service CI/CD

on:
  push:
    branches: [main]
    paths:
      - "user-service/**"
      - ".github/workflows/user-service-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "user-service/**"
      - ".github/workflows/user-service-ci.yml"

defaults:
  run:
    working-directory: user-service

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/nutmeals-user-service

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install lint tools
        run: pip install ruff black
      - name: Ruff
        run: ruff check .
      - name: Black (check only)
        run: black --check .

  test:
    runs-on: ubuntu-latest
    needs: lint
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: nutmeals
          POSTGRES_PASSWORD: nutmeals
          POSTGRES_DB: user_db_test
        ports: ["5434:5432"]
        options: >-
          --health-cmd="pg_isready -U nutmeals -d user_db_test"
          --health-interval=5s --health-timeout=5s --health-retries=10
      redis:
        image: redis:7-alpine
        ports: ["6381:6379"]
        options: >-
          --health-cmd="redis-cli ping" --health-interval=5s --health-timeout=5s --health-retries=10
    env:
      TEST_DATABASE_URL: postgresql+asyncpg://nutmeals:nutmeals@localhost:5434/user_db_test
      DATABASE_URL: postgresql+asyncpg://nutmeals:nutmeals@localhost:5434/user_db_test
      REDIS_URL: redis://localhost:6381/0
      CELERY_BROKER_URL: redis://localhost:6381/1
      CELERY_RESULT_BACKEND: redis://localhost:6381/2
      JWT_SECRET: ci-test-secret
      INTERNAL_SERVICE_TOKEN: ci-test-internal-token
      ENVIRONMENT: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: user-service/requirements.txt
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run Alembic migrations against test DB
        run: alembic upgrade head
      - name: Run tests with coverage
        run: pytest --cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=80
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: user-service/coverage.xml

  build-and-push:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU (for ARM64 emulation)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=raw,value=latest

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: user-service
          target: runtime
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production
    steps:
      - name: Deploy User Service
        run: |
          echo "Triggering deployment of ${{ env.IMAGE_NAME }} to the user-service target."
          echo "Replace this step with your actual deploy mechanism (e.g. OCI OKE rollout restart,"
          echo "ArgoCD sync, or a Terraform/Helm apply) -- kept as a placeholder here since the"
          echo "deployment target is infrastructure-specific."
        env:
          OCI_VAULT_SECRET_OCID: ${{ secrets.OCI_VAULT_SECRET_OCID }}

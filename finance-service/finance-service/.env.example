# Copy to .env for local docker-compose usage. NEVER commit a real .env file -
# production secrets are injected via OCI Vault + the deployment pipeline, not files.

FINANCE_JWT_SECRET_KEY=local-dev-secret-change-me
FINANCE_ENV=local

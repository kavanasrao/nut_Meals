[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=80
env =
    SEO_DATABASE_URL=sqlite+aiosqlite:///:memory:
    SEO_ENV=test
    SEO_JWT_PUBLIC_KEY=test-secret-not-real
